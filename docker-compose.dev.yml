services:
  mangadex-web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mangadex-downloader-web-dev
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=development
      - FLASK_DEBUG=True
    volumes:
      - ./downloads:/app/downloads
      - .:/app
    restart: unless-stopped
    networks:
      - mangadex-network

networks:
  mangadex-network:
    driver: bridge
